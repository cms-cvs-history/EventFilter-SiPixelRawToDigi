process DigiToRawTest = {

  source = PoolSource { 
    untracked string fileName = 'pixdigi1.root'
    untracked int32 maxEvents = 10
    untracked uint32 debugVebosity = 10
    untracked bool   debugFlag     = true
  }

  es_source = XMLIdealGeometryESSource {
    FileInPath GeometryConfiguration="Geometry/TrackerSimData/data/trackerSimConfiguration.xml"
    string rootNodeName = ""
  }

  es_module = TrackerGeometricDetESModule {}
  es_module = TrackerDigiGeometryESModule {}

  module DigiToRaw = SiPixelDigiToRaw {
    int32  Verbosity = 1
    string DigiProducer = "pixdigi"
  }

  module rawdata = PoolOutputModule {
    untracked string fileName = 'rawdata.root'
  }

#  es_source = SiPixelFedCablingTextBuilder {
#    untracked string fileName = 'pixelToFED.ascii'
#    }

  path p1 = { DigiToRaw, rawdata }
}
