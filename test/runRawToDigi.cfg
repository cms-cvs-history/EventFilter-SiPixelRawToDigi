process RawToDigiTest = {

source = PoolSource 
{
   untracked string fileName =  "rawdata.root"
   untracked int32 maxEvents = 5
}


# input service 
#source = DAQFileInputService
#{ 
#       string fileName = "rawdata.root"
#}

  es_source = XMLIdealGeometryESSource {
    FileInPath GeometryConfiguration="Geometry/TrackerSimData/data/trackerSimConfiguration.xml"
    string rootNodeName = ""
  }

  es_module = TrackerGeometricDetESModule {}
  es_module = TrackerDigiGeometryESModule {}

# define modules to be used 
module RawToDigi = SiPixelRawToDigi
{
       int32  Verbosity = 1
}

#provide a scheduler path
path p1 = { RawToDigi }      

}
